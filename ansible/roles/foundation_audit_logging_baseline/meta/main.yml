# SPDX-License-Identifier: AGPL-3.0-or-later
---
galaxy_info:
  role_name: foundation_audit_logging_baseline
  description: "Foundation layer 3 — audit-and-logging baseline (auditd, systemd-journald)."
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Fedora
      versions:
        - "44"
        - "45"

dependencies:
  - role: foundation_umask
  - role: foundation_sudo_roles
