# SPDX-License-Identifier: AGPL-3.0-or-later
---
galaxy_info:
  role_name: topic_avahi_daemon
  description: "avahi-daemon hardening (two drop-ins plus topic-owned CIL, mDNS responder)."
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Fedora
      versions:
        - "44"
        - "45"

dependencies:
  - role: foundation_umask
  - role: foundation_sudo_roles
  - role: foundation_selinux_cil_bootstrap
  - role: foundation_audit_logging_baseline
