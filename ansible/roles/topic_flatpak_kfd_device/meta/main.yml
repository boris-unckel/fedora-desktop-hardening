# SPDX-License-Identifier: AGPL-3.0-or-later
---
galaxy_info:
  role_name: topic_flatpak_kfd_device
  description: "bwrap bind-source stat of /dev/kfd under staff_t-launched Flatpak applications holding the dri device permission on Flatpak 1.18+ (single-rule topic-owned CIL module loaded at priority 400, no drop-in, no fcontext, no service touch; the patched denial is dontaudit-suppressed)."
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Fedora
      versions:
        - "44"
        - "45"

dependencies:
  - role: foundation_umask
  - role: foundation_sudo_roles
  - role: foundation_selinux_cil_bootstrap
  - role: foundation_audit_logging_baseline
