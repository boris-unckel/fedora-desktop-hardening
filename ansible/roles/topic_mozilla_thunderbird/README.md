<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
# topic_mozilla_thunderbird

## Purpose

Topic role that installs Mozilla Thunderbird as a Fedora-OCI-installed system-wide Flatpak ref, writes a system-wide sandbox-override that scopes the application's filesystem and socket access down from its Fedora-Hardened Manifest defaults (`filesystems=xdg-download;!host;!home;`, `sockets=wayland;!x11;`), writes three rows in the Flatpak permission database at `/var/lib/flatpak/db/org.freedesktop.impl.portal.access` to hard-deny the camera, microphone, and location surfaces under the application ID `net.thunderbird.Thunderbird`, and removes the canonical four-package Mozilla-stack RPM tree (`thunderbird`, `thunderbird-librnp-rnp`, `mozilla-openh264`, `mozilla-filesystem`) plus any trailing orphans picked up by `dnf autoremove`. The role-switched mutating calls (`flatpak install --system`, `flatpak override --system`, `flatpak permission-set`) transit `sudo -r sysadm_r -t sysadm_t`. The role ships **no** systemd unit, **no** systemd drop-in, **no** `/etc/profile.d/` script, **no** configuration file under `/etc/thunderbird/` or `/etc/mozilla/`, **no** polkit rule, **no** sudoers fragment, **no** desktop-entry override under `/usr/share/applications/`, **no** SELinux CIL module, **no** `semanage fcontext` mapping, **no** `restorecon` invocation, **no** file-label change, **no** system-bus `systemctl restart`, and **no** per-profile preference seed (the application has no VAAPI-equivalent operator-side surface in this topic's framing). The full topic end-state and the verify discipline are documented in `docs/reference/topics/mozilla-thunderbird.md`.

The role's preflight performs nine applicability checks: OS family (Fedora >= 44), required-package presence (`flatpak`, `ostree`), operator-mapping note (the role completes on `unconfined_u` with an informational note rather than aborting — the end-state behaves identically, only the surrounding hardening posture is owned by separate articles), Fedora-OCI remote presence (fail-fast on a missing remote or a remote whose URL does not contain `registry.fedoraproject.org`), OCI-pull DBus SELinux allow-rule presence (`sesearch -A -s sysadm_t -t unconfined_dbusd_t -c unix_stream_socket -p connectto`; fail-fast on a missing rule with an error message naming the `flatpak-oci-pull-dbus` sibling topic), audio-sandbox SELinux allow-rule presence (`sesearch -A -s staff_t -t device_t -c dir -p mounton`; fail-fast on a missing rule with an error message naming the `flatpak-audio-sandbox` sibling topic), SONAME-aware reverse-dependency probe against the four-package RPM removal set (per-package `rpm -ql | grep '\.so'` + `rpm -q --whatrequires '<libfoo.so.N>()(64bit)'`; fail-fast on a non-empty reverse-dependency list), and live-Thunderbird-process probes against both the host-side `/usr/(lib64|bin)/thunderbird/` pattern and the sandbox-side `net\.thunderbird\.Thunderbird` pattern (fail-fast on a non-empty result; the profile-seeding stage relies on no Thunderbird process holding the profile lock at apply time).

## Variables

| Name | Default | Purpose |
|---|---|---|
| `topic_mozilla_thunderbird_required_packages` | `[flatpak, ostree]` | Required-package preflight; fail-fast on a missing entry. |
| `topic_mozilla_thunderbird_expected_seuser_substring` | `staff_u` | Operator runtime SELinux mapping anchor (informational; does not abort on mismatch). |
| `topic_mozilla_thunderbird_fedora_remote_name` | `fedora` | The Fedora-OCI remote name on the system-wide Flatpak install. |
| `topic_mozilla_thunderbird_fedora_remote_url_substring` | `registry.fedoraproject.org` | The canonical Fedora-OCI URL substring; preflight fail-fasts when the configured `fedora` remote URL does not match. |
| `topic_mozilla_thunderbird_application_id` | `net.thunderbird.Thunderbird` | The application ref ID. |
| `topic_mozilla_thunderbird_branch` | `stable` | The Fedora-OCI branch to track (the role does not pin a specific point release). |
| `topic_mozilla_thunderbird_rpm_removal_set` | `[thunderbird, thunderbird-librnp-rnp, mozilla-openh264, mozilla-filesystem]` | Canonical four-package RPM removal set: two Thunderbird-specific entries plus two Mozilla-stack-shared entries (idempotent on a host where the sibling browser topic has already removed the shared entries). |
| `topic_mozilla_thunderbird_sandbox_override` | `{filesystems_deny: [host, home], filesystems_allow: [xdg-download], sockets_deny: [x11], sockets_allow: [wayland]}` | Sandbox-override structured mapping. Drives the byte-exact `flatpak override --system` invocation. |
| `topic_mozilla_thunderbird_profiles` | `[]` | Operator-declared list of per-host Thunderbird profile directory names. Empty by default; the optional profile-snapshot and profile-seeding stages run only when the operator populates this list and sets the snapshot path. |
| `topic_mozilla_thunderbird_external_snapshot_path` | `''` | Operator-side path to a host-side profile-tree archive (operator pre-deploy responsibility). The seeded archive carries the operator's full OpenPGP private key material; the path must be on operator-trusted storage. Documented but not consumed automatically by the rollback. |
| `topic_mozilla_thunderbird_portal_permission_camera` | `no` | Camera-surface portal-permission posture; the role writes a `flatpak permission-set ... <value>` invocation per loop entry. Override to `'ask'` to skip the hard-deny on this surface. |
| `topic_mozilla_thunderbird_portal_permission_microphone` | `no` | Microphone-surface portal-permission posture (same opt-out semantics). |
| `topic_mozilla_thunderbird_portal_permission_location` | `no` | Location-surface portal-permission posture (same opt-out semantics). |
| `topic_mozilla_thunderbird_portal_permission_background` | `ask` | Background-surface posture (informational; the role does not write a permission-set entry for the background surface — a hard-deny would suppress new-mail notifications). |
| `topic_mozilla_thunderbird_repo_config_path` | `/var/lib/flatpak/repo/config` | The ostree-layer INI config file the preflight reads to confirm the `fedora` remote's URL transport. The OCI transport does not require a `collection-id=` binding. |

## Dependencies

- `foundation_sudo_roles` (Layer 1) — every privileged step transits the `staff_u -> sysadm_r -> sysadm_t` role-switch surface (the system-install, the sandbox-override write, the three portal-permission writes, the override-file content read in the probe and verify, and the role-switched `ausearch` against the AVC stream). Plain `sudo` from a `staff_u`-mapped login lands in `staff_sudo_t`, which lacks both the DAC capability against the UMASK-027-locked Flatpak system store and the SELinux write-transition the policy expects against the system-wide Flatpak store, the system-wide override directory, and the system-wide portal-permission database.
- `foundation_audit_logging_baseline` (Layer 3) — the verify discipline asserts boot-clean AVC posture against the application's process tree (`thunderbird`, `bwrap`, `flatpak.*net\.thunderbird\.Thunderbird` keyword set) using the four-tool diagnosis loop (`ausearch`, `audit2why`, `audit2allow`, `sealert`) documented at the Layer 3 surface.

The role intentionally does not depend on the UMASK foundation (the role does not write a fresh file under operator UMASK influence in any place where a non-operator daemon would later need to read it; the override file is mutated in place by the `flatpak override --system` code path which preserves mode and owner by design, the portal-permission database file is mutated in place by the `flatpak permission-set` code path which preserves mode and owner by design, and no per-profile preference seed is shipped) or the SELinux CIL bootstrap (no CIL module is shipped). The dependency set is deliberately narrower than the four-Foundation set used by other Flatpak topics.

## Tags

- `topic_mozilla_thunderbird` — all role tasks.
- `preflight` — preflight checks only (OS family, required-package presence, operator-mapping note, Fedora-OCI remote presence, OCI-pull DBus SELinux allow-rule presence, audio-sandbox SELinux allow-rule presence, SONAME-aware reverse-dependency probe, live-Thunderbird-process probes).
- `probe` — read-only probe (`files/probe.sh`).
- `apply` — RPM removal, system-install, sandbox-override write, three-row portal-permission hard-deny, conditional portal-cache reset, optional profile-seeding, MIME-default informational read.
- `verify` — Soll/Ist verification (`files/verify.sh`).

## Idempotence notes

- The role is idempotent in the Ansible-`--check`-reports-zero-changes sense. The system-install task wraps `flatpak install -y --system fedora net.thunderbird.Thunderbird` in a `creates: /var/lib/flatpak/app/net.thunderbird.Thunderbird` style guard; on a host already carrying the application ref the install is a Flatpak-internal no-op and the task reports `changed=false`. On a re-run with a newer upstream point release available at the Fedora-OCI remote, the install promotes the host's deployed branch to the latest available point release; the `creates:` guard is conservative against the install-or-noop boundary.
- The sandbox-override task wraps the `flatpak override --system` invocation in a `changed_when` predicate that re-reads `/var/lib/flatpak/overrides/net.thunderbird.Thunderbird` after the call and reports `changed=true` only if the file content was added or modified.
- The three portal-permission tasks (camera, microphone, location) wrap each `flatpak permission-set` invocation in a `changed_when` predicate that compares the post-call `flatpak permission-list` row against the pre-call row and reports `changed=true` only if the row changed.
- The post-override portal-cache reset is gated on the registered fact `__topic_mozilla_thunderbird_overrides_changed`, set to `true` only if the override task or any of the three portal-permission tasks reported `changed=true`. The reset is encoded as a regular task with a `when:` clause, **not** as a handler — handlers fire after the entire play and would defer the cache reset past the post-deploy verify window.
- The four-package RPM removal and the trailing `dnf autoremove` are issued via `ansible.builtin.dnf` with `state: absent` and `autoremove: true`; on a host already in the end-state both calls are DNF-internal no-ops. On a host where the sibling Mozilla-stack browser topic has already removed the two shared packages (`mozilla-openh264`, `mozilla-filesystem`), the per-entry state is `absent` from the start and the dnf module reports `changed=false` against them.
- The `rescue:` block on the modify `block:` does **not** auto-rollback. The byte-exact rollback verb (the four-call sequence `flatpak permission-reset`, `flatpak override --system --reset`, `flatpak uninstall -y --system`, `dnf install -y` of the four-package set, plus operator-side restoration of the host-side profile tree from the operator's external snapshot if applicable) is the operator's deliberate decision and is documented in the Topic Reference under Recovery posture; an automatic rollback would be the wrong default.
- Boot-failure risk is **structurally zero**: the application is a post-login user-application; a Flatpak-side or sandbox-side misconfiguration is reversible from `sysadm_t` without rebooting; the application's bwrap-launched process tree has no dependency on `init_t` and cannot block the boot path. The Recovery-Pointer banner in the Topic Reference is included for tree consistency.
- On a correctly applied host, `--check` reports zero changes (every modify task reports `changed=false` and the conditional portal-cache reset is skipped on the `when:` clause). Stated as a claim, not a guarantee.
