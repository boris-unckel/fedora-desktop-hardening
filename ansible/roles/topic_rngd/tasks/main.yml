# SPDX-License-Identifier: AGPL-3.0-or-later
---
# Role: topic_rngd
# Pattern: Probe -> Modify -> Verify
# Idempotent: yes (re-applying does not change state on a correctly
# configured host; see README.md "Idempotence notes").
#
# Modify ordering invariant:
#   1. Push CIL source to /usr/local/share/selinux/.
#   2. Install CIL module via semodule -X 400 -i (handler).
#   3. flush_handlers — the CIL must be loaded before the NNP drop-in.
#   4. Push the three drop-in INI files.
#   5. restorecon on the drop-in files (handler).
#   6. Daemon-reload + restart (handlers).
# A change to any of the four shipping artefacts notifies the
# appropriate handler chain. Live-state probe is read-only.

- name: "topic_rngd | preflight"
  ansible.builtin.import_tasks: preflight.yml
  tags:
    - topic_rngd
    - preflight

- name: "topic_rngd | probe (read-only)"
  ansible.builtin.script: "files/probe.sh"
  changed_when: false
  failed_when: false
  register: probe_result
  tags:
    - topic_rngd
    - probe

- name: "topic_rngd | apply configuration"
  block:

    - name: "topic_rngd | CIL publish directory present"
      ansible.builtin.file:
        path: "{{ topic_rngd_cil_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"
      become: true

    - name: "topic_rngd | install CIL source"
      ansible.builtin.copy:
        src: "{{ topic_rngd_cil_module_name }}.cil"
        dest: "{{ topic_rngd_cil_dir }}/{{ topic_rngd_cil_module_name }}.cil"
        owner: root
        group: root
        mode: "0644"
      become: true
      notify:
        - "topic_rngd cil changed"

    - name: "topic_rngd | flush handlers (CIL must be loaded before NNP drop-in)"
      ansible.builtin.meta: flush_handlers

    - name: "topic_rngd | drop-in directory present"
      ansible.builtin.file:
        path: "{{ topic_rngd_dropin_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"
      become: true

    - name: "topic_rngd | install 99-hardening.conf"
      ansible.builtin.copy:
        src: "99-hardening.conf"
        dest: "{{ topic_rngd_dropin_dir }}/99-hardening.conf"
        owner: root
        group: root
        mode: "0644"
      become: true
      notify:
        - "topic_rngd dropin changed"

    - name: "topic_rngd | install 99-nnp.conf"
      ansible.builtin.copy:
        src: "99-nnp.conf"
        dest: "{{ topic_rngd_dropin_dir }}/99-nnp.conf"
        owner: root
        group: root
        mode: "0644"
      become: true
      notify:
        - "topic_rngd dropin changed"

    - name: "topic_rngd | install 99-process-restrict.conf"
      ansible.builtin.copy:
        src: "99-process-restrict.conf"
        dest: "{{ topic_rngd_dropin_dir }}/99-process-restrict.conf"
        owner: root
        group: root
        mode: "0644"
      become: true
      notify:
        - "topic_rngd dropin changed"

    - name: "topic_rngd | read live MainPID"
      ansible.builtin.command:
        argv:
          - systemctl
          - "show"
          - "-p"
          - "MainPID"
          - "--value"
          - "rngd.service"
      register: live_main_pid
      changed_when: false

    - name: "topic_rngd | read live SELinux domain"
      ansible.builtin.shell: >
        awk -F: '{print $3}' /proc/{{ live_main_pid.stdout | trim }}/attr/current
      args:
        executable: /bin/bash
      register: live_domain
      changed_when: false
      failed_when: false
      when: (live_main_pid.stdout | trim) not in ['', '0']
      become: true
      become_flags: "-r sysadm_r -t sysadm_t"

    - name: "topic_rngd | read live UID/GID"
      ansible.builtin.shell: >
        awk '/^Uid:/{print "Uid="$2} /^Gid:/{print "Gid="$2}'
        /proc/{{ live_main_pid.stdout | trim }}/status
      args:
        executable: /bin/bash
      register: live_uid_gid
      changed_when: false
      failed_when: false
      when: (live_main_pid.stdout | trim) not in ['', '0']

    - name: "topic_rngd | read AVC count since boot"
      ansible.builtin.shell: >
        ausearch -m AVC,USER_AVC -ts boot
        | grep -cE '(rngd_t|nnp_transition|rngd)'
        || true
      args:
        executable: /bin/bash
      register: avc_count
      changed_when: false
      failed_when: false
      become: true
      become_flags: "-r sysadm_r -t sysadm_t"

    - name: "topic_rngd | read SECCOMP count since boot"
      ansible.builtin.shell: >
        ausearch -m seccomp -ts boot
        | grep -c 'comm="rngd"'
        || true
      args:
        executable: /bin/bash
      register: seccomp_count
      changed_when: false
      failed_when: false
      become: true
      become_flags: "-r sysadm_r -t sysadm_t"

    - name: "topic_rngd | read post-deploy entropy_avail"
      ansible.builtin.shell: >
        cat {{ topic_rngd_entropy_avail_path }} || true
      args:
        executable: /bin/bash
      register: post_deploy_entropy
      changed_when: false
      failed_when: false

    - name: "topic_rngd | report live state"
      ansible.builtin.debug:
        msg: >
          rngd MainPID={{ live_main_pid.stdout | trim }},
          SELinux domain={{ (live_domain.stdout | default('?')) | trim }},
          live UID/GID={{ (live_uid_gid.stdout | default('?')) | trim | replace('\n', ' ') }},
          AVC count since boot={{ (avc_count.stdout | default('0')) | trim }},
          SECCOMP count since boot={{ (seccomp_count.stdout | default('0')) | trim }},
          entropy_avail={{ (post_deploy_entropy.stdout | default('?')) | trim }}.

  rescue:

    - name: "topic_rngd | apply failed; do not auto-rollback"
      ansible.builtin.fail:
        msg: |
          topic_rngd apply step failed. Auto-rollback is intentionally
          not performed; rolling back the four artefacts requires an
          operator decision about which stage to revert (process-
          restrictions only, or NNP plus CIL unload as well, or all
          three drop-ins). See the topic Reference under "Verification"
          for the three-stage rollback sequence and the recover-from-
          boot-failure how-to for the boot-failure variant.

  tags:
    - topic_rngd
    - apply

- name: "topic_rngd | flush handlers before verify"
  ansible.builtin.meta: flush_handlers
  tags:
    - topic_rngd
    - apply

- name: "topic_rngd | verify (Soll/Ist)"
  ansible.builtin.script: "files/verify.sh"
  changed_when: false
  register: verify_result
  failed_when: verify_result.rc != 0
  tags:
    - topic_rngd
    - verify
