# SPDX-License-Identifier: AGPL-3.0-or-later
---
galaxy_info:
  role_name: topic_udisks2
  description: "udisks2 mount-manager hardening (three drop-in profile, no custom CIL)."
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Fedora
      versions:
        - "44"
        - "45"

dependencies:
  - role: foundation_umask
  - role: foundation_sudo_roles
  - role: foundation_audit_logging_baseline
